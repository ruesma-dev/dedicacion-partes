<!-- docs/referencia/03_partes.md -->
# partes — vive en `azure-apps`, no aquí

> Puntero creado el 2026-08-19 al instalar el arnés. Documento real:
> `C:\Users\pgris\PycharmProjects\azure-apps\partes.md`.

El sistema de partes diarios. Escribe en las MISMAS tablas de Sigrid
(`hmo` / `hmores`) que este proyecto, con otro prefijo de `synckey`. Su
documento recoge las trampas de escritura ya pagadas (lotes de 15, `tex`
como TEXT, `MAX(ide)+1` con `UPDLOCK`, localizar el parte por `cod` y `tip`).

No se copia aquí: la única copia es la de `azure-apps`, y dos copias
divergen siempre. Si al trabajar descubres que ese documento está
desactualizado, corrígelo allí en el mismo trabajo.
